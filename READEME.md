# go agrope/
docker-compose build

docker-compose up -d


# check localhost for result

docker exec -it -u dev sf5_php bash

#to acces the core of file and here use composer to create or manage the project

# after cd sf5/
 composer install 

 all work will done here 
